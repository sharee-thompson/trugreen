import { getBaseUrl } from "../../../utils"
const high =  getBaseUrl + '/highIntentPath';
const med = getBaseUrl + '/medIntentPath';
const low = getBaseUrl + '/lowIntentPath';

const forNow =
  "https://tru-g-2025.netlify.app/iframe.html?id=pages-landing-test--default&viewMode=story";

export = [
    high,
    med,
    low,
    forNow
]  
