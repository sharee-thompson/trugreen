name: Functional Validation

on:
  workflow_dispatch:
    inputs:
      env:
        description: Target environment
        required: true
        default: prod
        type: choice
        options:
          - dev
          - qa
          - prod

  schedule:
    # Runs daily at 6:20 AM Central (11:20 UTC). GitHub cron uses UTC.
    - cron: "20 11 * * *"

permissions:
  contents: write

concurrency:
  group: dashboard-publish
  cancel-in-progress: false

jobs:
  functional-validation:
    name: Run Functional Validation
    timeout-minutes: 45
    runs-on: ubuntu-latest

    env:
      ENV: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.env || 'prod' }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4.1.6
        with:
          fetch-depth: 1

      - name: Restore existing dashboard contents
        run: |
          mkdir -p dashboard
          git fetch origin gh-pages --depth=1 || true
          if git rev-parse --verify origin/gh-pages >/dev/null 2>&1; then
            git --work-tree=dashboard checkout origin/gh-pages -- index.html
          fi

      - name: Setup Node
        uses: actions/setup-node@v4.0.1
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run buy flow original sanity test
        id: run-functional-validation
        run: |
          npx playwright test -g @functional

      - name: Save functional report for dashboard
        if: always()
        run: |
          mkdir -p dashboard/functional
          echo "{\"timestamp\":\"$(date -u +%Y-%m-%dT%H:%M:%S.000Z)\"}" > dashboard/functional/last-run.json
          if [ -d "playwright-report" ]; then
            rm -rf dashboard/functional/playwright-report
            cp -R playwright-report dashboard/functional/
          fi

      - name: Build dashboard homepage
        if: always()
        run: node scripts/build-dashboard-index.js

      - name: Upload functional Playwright report artifact
        if: always()
        uses: actions/upload-artifact@v4.3.1
        with:
          name: functional-playwright-report
          path: playwright-report/
          retention-days: 1
          if-no-files-found: warn

      - name: Upload functional JSON report artifact
        if: always()
        uses: actions/upload-artifact@v4.3.1
        with:
          name: functional-json-report
          path: playwright-report.json
          retention-days: 1
          if-no-files-found: warn

      - name: Clean up old reports
        if: always()
        run: bash scripts/cleanup-old-reports.sh

      - name: Publish functional report to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./dashboard
          keep_files: true

      # TEMPORARILY DISABLED: Teams notification
      # - name: Notify Teams on functional failure
      #   if: ${{ always() && steps.run-functional-validation.outcome == 'failure' }}
      #   uses: ./.github/actions/teams-notify
      #   with:
      #     webhook_url: ${{ secrets.TEAMS_WEBHOOK_URL }}
      #     suite_name: Functional Validation
      #     status: ${{ steps.run-functional-validation.outcome }}
      #     report_path: functional/playwright-report/index.html
